import {Component} from 'angular2/core';

@Component({
  selector: 'ButtonBase',
  template: `
    <button type="button" class="btn btn-default">
      Button
    </button>
  `
})

export class ButtonBase {}